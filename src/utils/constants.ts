const CANVAS_GRID_PATTERN_ID = "CANVAS_GRID_PATTERN_ID";

const CANVAS_GRID_SIZE = 20;

const RECT_NODE_SIZE = {
  width: 80,
  height: 80
}

const CIRCLE_NODE_SIZE = {
  radius: 45
}

const IN_ANCHOR_SIZE = {
  width: 8,
  height: 20
}

const OUT_ANCHOR_SIZE = {
  radius: 9
}

export {
  CANVAS_GRID_PATTERN_ID,
  CANVAS_GRID_SIZE,
  RECT_NODE_SIZE,
  CIRCLE_NODE_SIZE,
  IN_ANCHOR_SIZE,
  OUT_ANCHOR_SIZE
};